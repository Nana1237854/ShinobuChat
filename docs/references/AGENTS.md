## Agent skills

### Issue tracker

GitHub Issues on `Nana1237854/ShinobuChat`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context — `CONTEXT-MAP.md` at root, per-context `CONTEXT.md` under `app/` and `frontend/`. See `docs/agents/domain.md`.
